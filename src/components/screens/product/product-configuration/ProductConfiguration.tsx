import { FC } from 'react'
import styles from './productConfiguration.module.scss'

const ProductConfiguration: FC = () => {
	return <div>ProductConfiguration</div>
}

export default ProductConfiguration
